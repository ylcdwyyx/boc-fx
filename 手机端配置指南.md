# 手机端配置指南

数据已经跑通，下面是手机上的操作步骤。全程只需装一个 App。

---

## 一、先验证数据源（30 秒，必做）

用手机浏览器打开：

```
https://ylcdwyyx.github.io/boc-fx/
```

应该看到一个卡片，显示"美元 现钞买入价 669.xx"。

- ✅ **能正常显示** → 数据源没问题，继续往下做
- ❌ **打不开 / 一直转圈** → 跳到文末「常见问题」

> 这个网页本身也可以直接用：在浏览器里「添加到主屏幕」，就得到一个和"跨境go"组件类似的一键查看入口。

---

## 二、方案 A：KWGT 桌面小组件（推荐，效果最好）

### 1. 安装 KWGT

应用商店搜索 **KWGT**（全名 Kustom Widget Maker）。
免费版可以先试，够用就不必买 Pro。

### 2. 把组件放到桌面

长按手机桌面空白处 → **小组件 / 窗口小工具** → 找到 **KWGT** → 选 **4×1**（或 2×1）→ 拖到桌面。

### 3. 进入编辑

点一下刚放上去的 KWGT 占位组件 → 会自动打开 KWGT 编辑器。
在编辑器里点 **+** → 选择 **文本（Text）**。

### 4. 填入数据公式

在文本图层的 **Text** 输入框里，填入下面这段（**开头的 `$` 和结尾的 `$` 都必须有**）：

```
$wg("https://ylcdwyyx.github.io/boc-fx/rate.json", json, .currencies.usd.cash_buy)$
```

返回桌面，数字应该就出现了。常用的几条公式：

| 想显示的内容 | 公式 |
| --- | --- |
| 美元现钞买入价 | `$wg("https://ylcdwyyx.github.io/boc-fx/rate.json", json, .currencies.usd.cash_buy)$` |
| 美元现钞卖出价 | `$wg("https://ylcdwyyx.github.io/boc-fx/rate.json", json, .currencies.usd.cash_sell)$` |
| 牌价更新时间 | `$wg("https://ylcdwyyx.github.io/boc-fx/rate.json", json, .updated)$` |

### 5. 做得好看一点

在编辑器里选中文本图层，按需要调整：

- **字体大小 / 颜色 / 粗细** → 在字体相关设置里改，把买入价调大最醒目
- **位置** → 在位置设置里调 X / Y 偏移
- **再加几行字** → 再点 **+** 添加文本图层，用上面的公式表拼出你要的版式

推荐版式（三个文本图层）：

```
美元现钞          ← 固定文字，小号灰色
669.55            ← 买入价公式，大号加粗
卖 672.36         ← 固定文字"卖 " + 卖出价公式，中号
```

### 6. 保存

返回桌面即自动保存。

---

## 三、让数据保持更新

两件事会影响刷新，建议都设置一下：

**1. 允许 KWGT 后台运行**
系统设置 → 应用 → KWGT → 电池 → 选「不受限制 / 允许后台活动」。
安卓默认会限制后台，不设置的话自动刷新会不及时。

**2. 给组件加一个点击刷新**
在 KWGT 编辑器里选中组件 → **Touch（触摸）** 标签 → 把点击动作设为「刷新 / Refresh」。
以后想看最新价，点一下组件就行。

> 说明：中行牌价本身在非交易时段是不变的，所以平时看到的是上一次的数值属正常现象，点一下刷新即可确认。

---

## 四、方案 B：网页快捷方式（零成本备选）

不想装 App 的话：手机浏览器打开 `https://ylcdwyyx.github.io/boc-fx/` → 菜单 → **添加到主屏幕**。

之后桌面会多一个图标，点开就是一屏大字汇率，页面每 60 秒自动刷新。
不如小组件"一眼可见"，但完全零配置、零成本。

---

## 五、常见问题

**Q：`github.io` 打不开或很慢？**
先换个网络试试（WiFi ↔ 流量）。这是国内访问 GitHub 的偶发问题，通常重试即可。
如果长期打不开，告诉我，我改成本地方案（数据存在手机里，不依赖任何外部服务）。

**Q：组件上直接显示 `$wg(...)$` 原文？**
说明公式没被识别。检查两点：公式**开头和结尾都要有 `$`**；引号必须是**英文引号**。

**Q：数字一直是旧的？**
依次检查：① 点一下组件手动刷新；② 系统里给 KWGT 关了电池优化；③ 用手机浏览器打开上面那个网址，确认数据源本身是通的。

**Q：想加港币、欧元等其他币种？**
把公式里的 `usd` 换成 `hkd`（港币）、`eur`（欧元）、`jpy`（日元）、`gbp`（英镑）、`aud`（澳元）、`cad`（加元）、`sgd`（新元）、`chf`（瑞郎）、`nzd`（新西兰元）即可。

**Q：数据多久更新一次？**
云端每 30 分钟自动抓一次，你什么都不用管。手机端刷新的快慢取决于上面第三步的设置。
